meta {
  name: Resynchronize TOTP ot HOTP
  type: http
  seq: 1
}

post {
  url: https://{{identityTenantID}}.id.cyberark.cloud/Oath/ResychronizeOathToken
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{identityToken}}
}

body:json {
  {
    "firstCode": "string",
    "secondCode": "string",
    "tokenId": "string"
  }
}
