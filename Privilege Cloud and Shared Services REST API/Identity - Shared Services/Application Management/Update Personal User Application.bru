meta {
  name: Update Personal User Application
  type: http
  seq: 22
}

post {
  url: https://{{identityTenantID}}.id.cyberark.cloud/UPRest/UpdatePersonalApplication
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{identityToken}}
}

body:json {
  {
    "appName": "string",
    "appkey": "string",
    "appDescription": "string",
    "notes": "string",
    "appUrl": "string"
  }
}
