meta {
  name: Get Authentication Profile
  type: http
  seq: 2
}

post {
  url: https://{{identityTenantID}}.id.cyberark.cloud/AuthProfile/GetProfile?uuid=authProfileUUID
  body: json
  auth: bearer
}

params:query {
  uuid: authProfileUUID
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{identityToken}}
}

body:json {
  {
    "uuid": "string"
  }
}
