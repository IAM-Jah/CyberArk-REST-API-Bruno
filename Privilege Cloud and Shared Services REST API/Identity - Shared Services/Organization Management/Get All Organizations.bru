meta {
  name: Get All Organizations
  type: http
  seq: 8
}

post {
  url: https://{{identityTenantID}}.id.cyberark.cloud/Org/ListAll
  body: none
  auth: bearer
}

params:query {
  ~format: Query
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{identityToken}}
}
