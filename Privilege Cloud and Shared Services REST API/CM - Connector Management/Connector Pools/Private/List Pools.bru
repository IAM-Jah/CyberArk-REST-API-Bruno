meta {
  name: List Pools
  type: http
  seq: 2
}

get {
  url: https://{{subdomain}}.cyberark.cloud/private/pools
  body: none
  auth: bearer
}

params:query {
  ~continuationToken: string
  ~filter: string
  ~order: string
  ~pageSize: string
  ~projection: string
  ~sort: string
}

headers {
  Content-Type: application/json
  tenant-id: tenantID
}

auth:bearer {
  token: {{identityToken}}
}
