meta {
  name: Get Password Value
  type: http
  seq: 9
}

post {
  url: https://{{subdomain}}.privilegecloud.cyberark.cloud/PasswordVault/API/Accounts/:accountID/Password/Retrieve/
  body: json
  auth: bearer
}

params:path {
  accountID: {{accountID}}
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{identityToken}}
}

body:json {
  {
      "reason": "Retrieved through Bruno",
      "TicketingSystem": "{{ticketingSystemName}}",
      "TicketId": "{{ticketID}}",
      //"Version": <version number>,
      "ActionType": "<showOrCopyOrConnect>",
      //"isUse": false,
      "Machine": "targetAddress"
  }
}
