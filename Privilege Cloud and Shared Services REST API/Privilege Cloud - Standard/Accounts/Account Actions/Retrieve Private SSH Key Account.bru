meta {
  name: Retrieve Private SSH Key Account
  type: http
  seq: 5
}

post {
  url: https://{{subdomain}}.privilegecloud.cyberark.com/PasswordVault/API/Accounts/:accountID/Secret/Retrieve/
  body: json
  auth: none
}

params:path {
  accountID: {{accountID}}
}

headers {
  Content-Type: application/json
  Authorization: {{PAMSessionToken}}
}

body:json {
  {
      "reason": "Retrieved with Bruno",
      "TicketingSystemName": "{{ticketingSystemName}}",
      "TicketId": "{{ticketID}}",
      //"Version": <version number>,
      "ActionType": "download",
      //"isUse": false,
      "Machine": "targetAddress"
  }
}
